{
  "lock": false,
  "fmt": {
    "lineWidth": 100,
    "singleQuote": false,
    "semiColons": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "tasks": {
    "check": "deno check **/*.ts",
    "test": "deno test --allow-env --allow-net ../tests"
  }
}
