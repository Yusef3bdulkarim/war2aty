<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The user photographs a paper; the image never leaves the device. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Required from F06-T14: the app calls Supabase Anonymous Auth and the
         analysis Edge Function. Offline, only extracted text is ever sent;
         online, the photo itself transits the Edge Function for secure
         processing and is never stored server-side (§7, F13). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ (API 33) requires this as a runtime permission before any
         notification can show — requested once, right before the first
         reminder that needs one is saved (F09-T09), never during onboarding. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- F09-T10/T13: reminders are scheduled inexact (`inexactAllowWhileIdle`,
         no SCHEDULE_EXACT_ALARM needed — see
         `FlutterLocalNotificationsPort`'s own doc comment for why). These two
         let flutter_local_notifications re-arm what it had scheduled after a
         reboot, so a reminder is not silently lost until the app happens to
         be reopened; `ReminderScheduler.reconcile` (F09-T13) is the second,
         app-level line of defence for the same problem. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- The app is usable without a camera (gallery fallback), so a device
         without one must still be able to install it. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- F12-T07 · Auto Backup is ON by default, and it would copy
         `war2aty.sqlite` — every saved paper's analysis in plain text: the
         summary, the key information, the amounts, the dates — to the user's
         Google Drive. The saved-papers screen promises the opposite in so
         many words: «بنحفظ الملخص والتفاصيل وصورة الورقة مشفّرة على جهازك.»
         — on YOUR device. That is the claim Auto Backup quietly breaks.

         It would also restore flutter_secure_storage's wrapped key material
         without the Keystore key that unwraps it, which that package's own
         README documents as an `InvalidKeyException: Failed to unwrap key`
         crash rather than a graceful degrade.

         Both are off here. `data_extraction_rules` is what covers
         device-to-device transfer on API 31+, which `allowBackup` alone does
         not; see that file for the reasoning and the tradeoff it accepts. -->
    <application
        android:label="${appName}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- F09-T10/T13: lets flutter_local_notifications re-schedule what it
             had pending after the device reboots. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
