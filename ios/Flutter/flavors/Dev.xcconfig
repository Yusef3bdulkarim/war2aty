// Flavor: dev
//
// INERT until wired into Xcode build configurations — see ios/FLAVORS.md.
// These settings are the flavor-specific overrides a `*-dev` build
// configuration should apply on top of the base Debug/Release/Profile config.

FLUTTER_TARGET = lib/main_dev.dart
PRODUCT_BUNDLE_IDENTIFIER = com.war2aty.app.dev
APP_DISPLAY_NAME = ورقتي (Dev)
